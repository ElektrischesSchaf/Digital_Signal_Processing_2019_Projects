n=20
[x,t]=func1(n);
scatter(t,x);


function [signal,timestamp]=func1(n)
    signal=[]
    timestamp=[];
    if n<0
        return 
    end
    
    if n>20
        return
    end
    
    for i=1:n
        temp=(0.9)^i
        signal=[signal temp]
        timestamp=[timestamp i]
        %x[i]=signal
    end
end

function y=func2(x, n)
    if (n<0)
        return 0;
    elseif (n==0)
        return 0;
    else
        return 1.8*cos(pi/16)*func2(x, n-1)+0.81*fun
    end
end